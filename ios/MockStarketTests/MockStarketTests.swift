import Testing
@testable import MockStarket

@Suite("MockStarket Tests")
struct MockStarketTests {
    @Test("App launches successfully")
    func appExists() {
        // Placeholder test — replace with real tests as features solidify
        #expect(true)
    }
}
